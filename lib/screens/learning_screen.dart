import 'package:flutter/material.dart';

class LearningScreen extends StatelessWidget {
  const LearningScreen({super.key, required String subject});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("LearningScreen"),
    );
  }
}
